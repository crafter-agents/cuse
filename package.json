{
  "name": "cuse-cli",
  "version": "0.2.1",
  "type": "module",
  "description": "Cross-platform computer-use CLI. One verb, the right OS primitive.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/crafter-agents/cuse.git"
  },
  "homepage": "https://github.com/crafter-agents/cuse#readme",
  "bugs": {
    "url": "https://github.com/crafter-agents/cuse/issues"
  },
  "keywords": [
    "computer-use",
    "desktop-automation",
    "cli",
    "agents"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "bin": {
    "cuse": "./dist/cli.js"
  },
  "files": [
    "dist",
    "src",
    "skills",
    "README.md"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile --external koffi src/cli.ts --outfile dist/cuse",
    "build:node": "bun run scripts/build-node.ts",
    "prepack": "bun run build:node"
  },
  "devDependencies": {
    "electron": "^43.4.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "koffi": "^3.1.6"
  }
}
