{
  "name": "@crafter/cuse",
  "version": "0.1.0",
  "type": "module",
  "description": "Cross-platform computer-use CLI. One verb, the right OS primitive.",
  "bin": {
    "cuse": "./src/cli.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/cli.ts --outfile dist/cuse"
  }
}
