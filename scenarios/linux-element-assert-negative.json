{
  "version": 1,
  "name": "assert a live Linux GTK control's observed value (forced red)",
  "platforms": ["linux"],
  "vars": {},
  "defaultTimeoutMs": 20000,
  "steps": [
    { "type": "cuse", "action": "element", "args": ["zenity"], "options": { "role": "text" }, "saveAs": "target" },
    { "type": "assert", "actual": "${steps.target.data.value}", "operator": "eq", "expected": "the wrong text" }
  ]
}
